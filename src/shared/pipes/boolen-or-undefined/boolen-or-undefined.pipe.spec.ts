import { BoolenOrUndefinedPipe } from './boolen-or-undefined.pipe';

describe('BoolenOrUndefinedPipe', () => {
  it('should be defined', () => {
    expect(new BoolenOrUndefinedPipe()).toBeDefined();
  });
});
